package com.abelhu.leet

import org.junit.Test

class Code435 {
    private fun eraseOverlapIntervals(intervals: Array<IntArray>): Int {
        return 0
    }

    @Test
    fun test0() {
        assert(1 == eraseOverlapIntervals(arrayOf(intArrayOf(1, 2), intArrayOf(2, 3), intArrayOf(3, 4), intArrayOf(1, 3))))
    }
}